<?xml version="1.0" encoding="utf-8"?>
<!-- Copyright 2019 Tecnativa - Sergio Teruel
     License AGPL-3.0 or later (https://www.gnu.org/licenses/agpl). -->
<odoo>

    <template id="stock_info">
        <t t-if="line.product_id.inventory_availability == 'always_no_lock'">
            <t t-set="free_qty" t-value="line.product_id.free_qty"/>
            <t t-set="available_threshold" t-value="line.product_id.available_threshold"/>
            <div t-if="available_threshold > free_qty" t-attf-class="availability_messages text-danger">
                <t t-set="free_qty" t-value="free_qty if free_qty > 0.0 else 0.0"/>
                <span><t t-esc="free_qty"/> <t t-esc="line.product_uom.name"/> available</span>
            </div>
        </t>
    </template>

    <!-- Shopping Cart Lines -->
    <template id="cart_lines" inherit_id="website_sale.cart_lines">
        <xpath expr="//td[hasclass('td-qty')]" position="inside">
            <t t-call="website_sale_stock_available_display.stock_info"/>
        </xpath>
    </template>

</odoo>
